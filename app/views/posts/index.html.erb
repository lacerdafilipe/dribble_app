<h1>All posts</h1>
<% @post.each do |post| %>
  <p><%= link_to post.title, post%></p>
  <p><%= post.link%></p>
  <p><%= post.description%></p>
  <p>Created by <%= post.user.name%> | <%= post.created_at.strftime("%A, %b, %d")%></p>

  <hr>
<%end%>


<%= link_to "New Post", new_post_path(@post) %>
